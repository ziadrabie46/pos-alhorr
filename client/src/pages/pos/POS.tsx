import { useEffect, useState } from 'react';
export default function POS(){const [products,setProducts]=useState<any[]>([]); const [cart,setCart]=useState<any[]>([]); useEffect(()=>{fetch('/api/products').then(r=>r.json()).then(setProducts)},[]);
return <div className='p-4 grid lg:grid-cols-3 gap-4'><div className='lg:col-span-2'><h1 className='text-2xl font-bold mb-4'>المنتجات</h1><div className='grid grid-cols-2 lg:grid-cols-4 gap-3'>{products.map(p=><button key={p.id} className='honey-card text-right' onClick={()=>setCart(c=>[...c,{...p,qty:1}])}><div>{p.name}</div><div>{p.code}</div><div>{p.price} ر.س</div></button>)}</div></div><aside className='honey-card'><h2 className='font-bold text-xl'>السلة</h2>{cart.map((c,i)=><div key={i} className='flex justify-between'><span>{c.name}</span><span>{c.price}</span></div>)}</aside></div>}
